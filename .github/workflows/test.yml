name: tests
on:
  push:
    branches-ignore: [main]
    paths-ignore:
      - 'docs/**'
      - '*.md'
  workflow_dispatch:

jobs:
  unit-tests:
    uses: salesforcecli/github-workflows/.github/workflows/unitTest.yml@main

  # NUTs deshabilitados temporalmente hasta tener funcionalidad Salesforce real
  # Esto ahorra minutos de CI durante la fase de desarrollo inicial (solo types/constants)
  # TODO: Rehabilitar cuando implementemos parsers y lógica de deployment
  # nuts:
  #   needs: unit-tests
  #   uses: salesforcecli/github-workflows/.github/workflows/nut.yml@main
  #   secrets: inherit
  #   strategy:
  #     matrix:
  #       os: [ubuntu-latest, windows-latest]
  #     fail-fast: false
  #   with:
  #     os: ${{ matrix.os }}
